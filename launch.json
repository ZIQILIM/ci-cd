{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Launch CleaningServiceApp",
      "request": "launch",
      "mainClass": "CleaningServiceApp",
      "projectName": "CSIT314 PROJECT",
      "classpath": [
        // your compiled classes
        "${workspaceFolder}/bin",
        // **and** the MySQL driver:
        "${workspaceFolder}/lib/mysql-connector-j-8.0.33.jar",
        // Add the JDatePicker library:
        "${workspaceFolder}/lib/jdatepicker-1.3.4.jar"
      ]
    },
    {
      "type": "java",
      "name": "Launch DatabaseConnection",
      "request": "launch",
      "mainClass": "database.DatabaseConnection",
      "projectName": "CSIT314 PROJECT",
      "classpath": [
        // your compiled classes
        "${workspaceFolder}/bin",
        // **and** the MySQL driver:
        "${workspaceFolder}/lib/mysql-connector-j-8.0.33.jar",
        // Add the JDatePicker library:
        "${workspaceFolder}/lib/jdatepicker-1.3.4.jar"
      ]
    }
  ]
}